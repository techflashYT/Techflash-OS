// yes this file does mean I eventually plan to expand to these architectures at least, if not more,
// but for now the only supported arch is x86_64, and it's the only one I'm using.
#define ARCH_X86_64 0
#define ARCH_I386   1
#define ARCH_ARM32  2
#define ARCH_ARM64  3