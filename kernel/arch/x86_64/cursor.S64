.global _disableCursor
.type _disableCursor, @function
_disableCursor:
	pushf
	push %rax
	push %rdx

	mov $0x3d4, %dx
	mov $0xa, %al
	out %al, %dx

	inc %dx
	mov $0x20, %al
	out %al, %dx

	pop %rdx
	pop %rax
	popf
	ret
